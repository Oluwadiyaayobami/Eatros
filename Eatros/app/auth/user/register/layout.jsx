export const metadata = {
  title: "Create an Account | Get Started Today",
  description: "Fast, secure portal access for customers, vendors, and agents.",
  alternates: {
    canonical: '/register',
  }
};

export default function RegisterLayout({ children }) {
  return <>{children}</>;
}
