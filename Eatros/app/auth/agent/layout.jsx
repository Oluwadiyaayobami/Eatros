export const metadata = {
  title: "Join as an Agent | Earn & Grow with Our Network",
  description: "Register as an official agent. Partner with us to scale operations, manage client workflows, and unlock new growth opportunities.",
  alternates: {
    canonical: '/register/agent',
  }
};

export default function AgentRegisterLayout({ children }) {
  return <>{children}</>;
}
