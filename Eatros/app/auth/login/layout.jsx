export const metadata = {
  title: "Account Login | Access Your Portal",
  description: "Fast, secure portal access for customers, vendors, and agents.",
  alternates: {
    canonical: '/login',
  }
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
